<Window x:Class="FootballMatches.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FootballMatches"            
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MatchesVM />
    </Window.DataContext>
    
    <Grid>
        <StackPanel HorizontalAlignment="Left" Height="400" Margin="10,10,0,0" VerticalAlignment="Top" Width="774">
            <Label x:Name="lblTeams" Content="Teams" HorizontalAlignment="Center"/>
            <ComboBox x:Name="comboBoxTeams" ItemsSource="{Binding Path=Teams}" SelectedItem="{Binding Path=ChosenTeam}"/>
            <Button x:Name="btnShowMatches" Command="{Binding Path=ShowChosenTeamCommand}" Content="Show Matches" Margin="0,0,0.4,0"/>
            <Label x:Name="lblMatches" Content="Matches" HorizontalAlignment="Center"/>
            <ComboBox x:Name="comboBoxMatches" ItemsSource="{Binding Path=Matches}" SelectedItem="{Binding Path=ChosenMatch}"/>
            <Button x:Name="btnMakeBet" Content="Make a bet" Command="{Binding Path=MakeABetCommand}"/>
            <Label x:Name="lblTime" Content="{Binding Path=HoursTillNextMatch}"/>
        </StackPanel>
    </Grid>
</Window>
